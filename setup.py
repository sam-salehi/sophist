from setuptools import setup, find_packages

setup(
    name="sophist",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'watchdog',
        # other dependencies
    ]
) 